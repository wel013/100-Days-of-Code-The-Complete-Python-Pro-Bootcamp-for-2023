This is a program that uses kmeans clustering algorithm to detect a color palette (custom number of colors). 

I suggest using this program in google colab because it runs faster than local machines (at least in my case).

Future updates to come: 
Use bootstrap templates to make is look better!
